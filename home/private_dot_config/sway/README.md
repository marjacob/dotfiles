Audio
-----

Configured in [`audio.conf`](config.d/audio.conf).

### Requirements

```shell
xbps-install -Su pamixer wob
```

Notifications
-------------

Configured in [`mako.conf`](config.d/mako.conf).

## Requirements

```shell
xbps-install -Su mako
```

